{
  "name": "http-timeout-wrapper",
  "version": "1.0.0",
  "description": "Smart HTTP wrapper with exponential backoff retries, jitter, and circuit breaker pattern for resilient requests",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage",
    "prepublishOnly": "npm test",
    "example": "node examples/basic.js",
    "lint": "echo 'No linter configured'",
    "dry-run": "npm pack --dry-run"
  },
  "keywords": [
    "http",
    "retry",
    "timeout",
    "circuit-breaker",
    "backoff",
    "resilience",
    "fetch",
    "exponential-backoff",
    "jitter",
    "fault-tolerance",
    "retries",
    "http-client"
  ],
  "author": "AVA",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ava-avant-iconic/http-timeout-wrapper.git"
  },
  "homepage": "https://github.com/ava-avant-iconic/http-timeout-wrapper#readme",
  "bugs": {
    "url": "https://github.com/ava-avant-iconic/http-timeout-wrapper/issues"
  },
  "files": [
    "src/",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
